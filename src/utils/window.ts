/**
 * Objeto window do browser.
 *
 * Esse export foi criado para que o objeto window possa ser mockado posteriormente nos testes unitários.
 */
const browserWindow = window;

export default browserWindow;
